# Sample REST API with Database

you can check the requests by importing `insomnia.json` to your ![insomnia](https://insomnia.rest).

## Running locally

```shell
cp .env.example .env
docker-compose up -d
yarn build
yarn start
```
